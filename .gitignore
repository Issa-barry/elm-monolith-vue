/.phpunit.cache
/bootstrap/ssr
/node_modules
# public/build was committed on this branch for Hostinger's old native Git
# deploy (eau-la-maman.com, plain `git pull`, no npm build step). That domain
# is being retired in favor of elm-vitrine as its own independent Hostinger
# site — fello.eau-la-maman.com's deploy-hostinger-admin.yml already builds
# fresh assets in CI and never relied on the committed copy.
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/resources/js/actions
/resources/js/routes
/resources/js/wayfinder
/vendor
/playwright-report
/test-results
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed

# Code coverage
/coverage/
/coverage-report/
.auth/

# Claude Code local settings (jamais commités)
/.claude/settings.local.json
