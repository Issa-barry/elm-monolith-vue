<IfModule mod_rewrite.c>
    RewriteEngine On

    # Keep ACME/SSL challenges reachable.
    RewriteRule ^\.well-known/ - [L]

    # Route all requests to /public except when already under /public.
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
